// App-wide configuration constants (milliseconds)
export const ADD_TO_CART_DELAY = 100; // delay before performing add-to-cart (ms)
export const ADDED_STATE_DURATION = 1800; // how long the "Added" state stays visible (ms)
